# Optional server deployment
PSX_DB=psx_v2.db
# Add an LLM key only when you intentionally deploy a grounded server-side summarizer.
# Never embed private API keys in the Android APK.
LLM_PROVIDER=
LLM_API_KEY=
