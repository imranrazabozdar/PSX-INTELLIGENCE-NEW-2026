name: Phase 1 Triple-Barrier test — BB_OVERSOLD (manual, one-off)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests pandas numpy statsmodels

      - name: 🎯 Run Phase 1 Triple-Barrier test on BB_OVERSOLD
        working-directory: analysis
        env:
          LIBSQL_URL: ${{ secrets.LIBSQL_URL }}
          LIBSQL_AUTH_TOKEN: ${{ secrets.LIBSQL_AUTH_TOKEN }}
        run: python phase1_triple_barrier_test.py

      - name: 📤 Commit and push the generated report
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add analysis/phase1_triple_barrier_result.md
          git diff --cached --quiet && echo "No changes to commit" || git commit -m "Phase 1 Triple-Barrier test result for BB_OVERSOLD (automated)"
          git push origin HEAD:${{ github.ref_name }}
