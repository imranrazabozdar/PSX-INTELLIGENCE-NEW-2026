# fire_engine/'s actual runtime dependencies (verified: no module in this
# package imports pandas/numpy/streamlit/plotly/apscheduler/playwright --
# the scraper hits chart.scstrade.com's plain JSON API with `requests`,
# storage is stdlib sqlite3, everything else is pure Python).
pyyaml>=6.0
requests>=2.31.0

# Testing only
pytest>=7.4.0
pytest-cov>=4.1.0
